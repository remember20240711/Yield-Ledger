services:
  yield-ledger:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: yield-ledger
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
